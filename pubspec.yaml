name: chatbot
description: "A new Flutter project."
publish_to: "none"
version: 0.1.0

environment:
  sdk: ^3.8.1

dependencies:
  flutter:
    sdk: flutter

  flutter_bloc: ^8.1.3
  go_router: ^10.1.2
  file_picker: ^6.1.1
  get_it: ^7.6.4
  firebase_core: any
  firebase_auth: any
  cloud_firestore: any
  firebase_storage: any
  http: ^0.13.6
  bloc: ^8.1.4
  meta: ^1.16.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
